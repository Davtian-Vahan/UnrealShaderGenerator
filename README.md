# UnrealShaderGenerator
This is a test project that attempts to build a specific shader generation tool.
Script supports a version agnostic Unreal Engine material node generation.
The example script demonstrates a highly customizable striped circle shader.
